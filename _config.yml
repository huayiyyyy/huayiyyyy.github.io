# Site settings
title: Your Name
description: >-
  Personal blog — notes, projects and publications.
# 本仓库是 huayiyyyy.github.io（用户主页仓库），部署在根路径，baseurl 保持 ""
# 如果以后改用项目仓库（如 blog，部署到 username.github.io/blog/），改为 "/blog"
baseurl: ""
url: "https://huayiyyyy.github.io"

markdown: kramdown
highlighter: rouge

# 排除不需要编译的文件
exclude:
  - README.md
  - .git
